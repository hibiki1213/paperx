# e-ink ダッシュボード 要件定義

父への誕生日プレゼント。TRMNL BYOD 7.5" キットに自作ファームウェアを載せ、天気・日付・時刻・ニュース等を表示する卓上ダッシュボード。

---

## 1. ゴール / コンセプト

- 電源を入れたら何もしなくても、今日の天気・気温・日付時刻・ちょっとした読み物が一目でわかる卓上デバイス
- TRMNL のクラウドレンダリングは使わず、**完全自作の on-device ファームウェア**（Arduino/C++）
- USB給電でも電池でも動く**両対応**。設置場所を選ばせない
- 白黒（1bit）e-ink ならではの、コントラストの高い新聞的レイアウト

## 2. ハードウェア

| 項目 | 仕様 |
|---|---|
| キット | TRMNL BYOD 7.5" DIY kit |
| ドライバボード | Seeed XIAO ESP32-S3 PLUS |
| MCU | ESP32-S3（Xtensa LX7 デュアルコア）/ 16MB flash / 8MB PSRAM |
| ディスプレイ | 7.5" e-ink 800×480 モノクロ（1bit） |
| 接続 | FPC ケーブル（e-ink ドライバ ↔ XIAO） |
| 電池 | 2000mAh LiPo（ボード側に充電/給電回路） |
| 給電 | USB-C（給電 兼 書き込み） |

## 3. 動作モード（両対応・自動切替）

USB給電の有無を検出して自動でモードを切り替える。

### 3-A. USB給電モード（常時稼働）
- **常時起動したまま**。deep sleep に入らない
- **時計を毎分更新**（partial refresh / 部分更新で高速・無点滅）
- 右カラムのローテーションは **10分おき**
- 天気/ニュースのデータ取得は定期ポーリング（例: 10分間隔）
- ゴースト対策として一定回数の partial 後に full refresh を1回挟む

### 3-B. 電池モード（省電力）
- **15分ごとに wake → 描画 → deep sleep** を繰り返す
- wake のたびに full refresh（partial は起きている前提の機能のため）
- 右カラムのローテーションは **起床ごとに切替**（= 約15分ごと）
- deep sleep 中は RAM/PSRAM の内容は失われる前提。跨いで保持したい最小限の状態は `RTC_DATA_ATTR`（RTC slow memory, 約8KB）に置く
  - フレームバッファ（約47KB）は RTC memory に入らない → 跨いで保持しない。wake ごとに再描画

### 切替ロジック
- 起動時/定期的に USB 電源（VBUS）の有無を判定し、3-A / 3-B を選択
- 給電が外れたら次サイクルから電池モードへ、挿さったら常時モードへ

## 4. WiFi接続・プロビジョニング / オフライン動作

### 接続情報の投入方式 — キャプティブポータル（確定）
- ライブラリ: `tzapu/WiFiManager`
- 初回起動時、または保存済み認証情報で接続失敗時に、端末が自前のアクセスポイント（例: SSID `PaperX-Setup`）を立てる
- スマホ等でそのAPに接続するとキャプティブポータル（ブラウザ設定画面）が開き、自宅のSSID選択＋パスワード入力
- 入力した認証情報は **NVS（flash）に保存**。以降は自動接続
- AP起動中は e-ink に「PaperX-Setup に接続して設定してください」と案内画面を表示
- **設定モード復帰手段**: 起動時にボタン長押しで保存済み情報を破棄し、再びAPモードへ。ルーター交換・パスワード変更時に **PCに繋がずスマホだけで復帰**できる（必要なら電話で父を誘導）
- 利点: 再書き込み不要 / 認証情報をリポジトリに置かない

### 認証情報の保存場所
- **NVS（`Preferences` ライブラリ / flash）に保存**。deep sleep でも電源OFFでも消えない
- ⚠️ `RTC_DATA_ATTR`（RTCメモリ）は電源喪失で消えるため認証情報には使わない

### 接続ライフサイクル
- **給電モード**: 起動時に一度接続し、以後は接続維持
- **電池モード**: deep sleep でWiFi状態が失われるため、**15分ごとの wake で毎回ゼロから再接続**（~1〜5秒）。この再接続が電池モードの消費電力の主役 → 起床間隔の調整に影響。認証情報はNVSにあるので自動再接続

### ネット依存度（常時接続は不要）
- 時計: 内蔵RTCで動作。NTP同期は1日1回程度で十分（オフラインでも時刻は進む）
- 天気/ニュース: 10〜15分おきの取得。失敗しても直近値を保持

### オフライン時のグレースフル表示
- WiFi失敗・パスワード誤り・ルーター再起動中でも **画面を壊さない**
- 時計はRTCで継続表示
- 天気/ニュースは最後に取得できた値を表示し、小さく「更新 HH:MM」を併記
- 接続できない時だけ控えめな「オフライン」マークを出す（全面エラー画面にはしない）

## 5. 画面レイアウト（案2 / 3分割ダッシュボード・現行スタイル）

`mockups.html` が確定デザイン（800×480, 白黒, 現行=ベースラインのスタイリング）。2パネル＝ローテーションの両面。

```
┌─────────────────────────────────────────────┐
│ ヘッダ: 6月1日 月曜日           [ 7:15 ]      │  ← border-bottom 3px
├──────────────────────┬──────────────────────┤
│ HERO (天気・左, 共通)  │ 右カラム (ローテーション) │
│  ☔ 22°               │  ┌ 天気版: 3日予報    │
│  曇りのち雨           │  │  火 ☔ 26/18      │  height 282px
│  最高26° / 最低18°    │  │  水 ☁ 24/17      │
│  ┌─────────────┐     │  │  木 ☀ 27/19      │
│  │14時から雨 傘いる│   │  └ ニュース版: 見出し×3│
│  └─────────────┘     │     NHK / スポーツ / 地域 │
├──────────────────────┴──────────────────────┤
│ 今日は何の日 ― 気象記念日…       [🌙 月齢16.2] │  ← border-top 3px, height 86px
└─────────────────────────────────────────────┘
```

### ゾーン定義
1. **ヘッダ** — 左: 日付＋曜日 / 右: 時計（大きく, 64px相当）。下に 3px 罫線
2. **メイン (高さ282px)**
   - **HERO（左, 両版共通）**: 天気アイコン + 現在気温（特大）+ 天気概況 + 最高/最低 + 傘アドバイス枠
   - **右カラム（ローテーション）**:
     - **天気版**: 3日予報（曜日・アイコン・最高/最低）、1px罫線区切り
     - **ニュース版**: 見出し×3（ソース＋時刻＋見出しテキスト）
3. **フッタ (高さ86px, 両版共通)** — 左: 「今日は何の日」読み物 / 右: 月齢（月相SVG＋数値）

### 右カラム ローテーション
- **天気版 ↔ ニュース版** を交互に表示
- 給電時: 10分おき / 電池時: 起床ごと（≒15分）
- HERO（左）とフッタ（今日は何の日＋月齢）は両版で共通＝固定

### 1bit デザイン原則
- グレー無し。区切りは実線/点線の黒、または見出しは黒ベタ反転で焦点化
- 数字は等幅（tabular numerals）で桁位置を固定し、毎分更新でガタつかせない
- アイコンはアウトライン（線画）で1bit映えさせる

## 6. 表示コンテンツとデータソース

| コンテンツ | 内容 | ソース |
|---|---|---|
| 時計 | HH:MM（給電時は毎分更新） | NTP同期 + 内蔵RTC, TZ=`JST-9` |
| 日付・曜日 | M月D日 + 曜日 | RTC |
| 現在気温 | 大きく表示 | Open-Meteo（APIキー不要） |
| 天気概況 + アイコン | 晴/曇/雨 等 | Open-Meteo weather code → アイコン |
| 最高/最低 | 今日の最高/最低 | Open-Meteo |
| 降水タイミング + 傘アドバイス | 「14時から雨 傘いる」等 | Open-Meteo の時間別降水から導出 |
| 3日予報 | 翌日以降3日（曜日/アイコン/最高最低） | Open-Meteo daily |
| ニュース見出し | 見出し×3（ソース・時刻） | RSS（NHK等） |
| 月齢 | 月相SVG + 数値 | 計算（日付から算出, ネット不要） |
| 今日は何の日 / 一言 | 読み物 | 当面はベイク（端末内データ）。将来RSS等も検討 |

- **動的に広い字種が要るのはニュース見出しのみ**。読み物・名言・脳トレ漢字は curated でベイク可能 → フォントサブセット設計に影響

## 7. フォント（確定）

| 用途 | フォント | 備考 |
|---|---|---|
| 英数字・記号 | **Poppins**（Bold/SemiBold/Medium/Regular） | 時計・気温の特大数字が主役。幾何学サンセリフで1bit映え |
| 日本語 | **LINE Seed JP**（ExtraBold/Bold/Regular） | 均一ストロークの幾何学ゴシック。1bitでも荒れにくい |

- ライセンス: 両者とも OFL（Open Font License）。同梱・改変・組込み配布OK
- **1bit適性は検証済み**（`assets/ttf_1bit_preview.png`）: 1bit(実機相当) と アンチエイリアス版の差はごく僅か
- 実装方針:
  - 各 TTF を **使用サイズごとに固定ビットマップへ変換**（Adafruit `fontconvert` 等）。実機ではスケーリングしない
  - サイズ例: 時計 ~96-120px / 気温 64px / 見出し和文 40-64px / ラベル 26-30px / 本文・ニュース 20-22px
  - ニュース本文の最小行（20px）は **Bold か 22px** にして密な漢字の可読性を確保
  - フラッシュ節約のため**必要な字種だけサブセット化**（ニュースは字種が読めないため広め、その他はベイク文言ぶんに絞る）

## 8. 技術スタック

| 層 | 採用 |
|---|---|
| ビルド | PlatformIO（`board=seeed_xiao_esp32s3`, `platform=espressif32`, `framework=arduino`） |
| 言語 | C++ / Arduino |
| ディスプレイ駆動 | **GxEPD2**（`GxEPD2_750_T7`, 800×480 BW） |
| 描画 | **Adafruit_GFX**（+ 必要なら `U8g2_for_Adafruit_GFX`） |
| フォント | fontconvert 生成のビットマップフォント（Poppins / LINE Seed JP） |
| JSON | ArduinoJson |
| 天気API | Open-Meteo（HTTPS, キー不要） |
| 時刻 | NTP + ESP32 内蔵RTC, `setenv("TZ","JST-9")` |
| WiFiプロビジョニング | `tzapu/WiFiManager`（キャプティブポータル + 自前AP） |
| 設定保存 | `Preferences`（NVS / flash） |
| 省電力 | `esp_deep_sleep`, `RTC_DATA_ATTR` |
| ニュース | RSS取得 + 簡易XML/フィードパース |

## 9. リフレッシュ戦略（1bitの肝）

- **Full refresh**: 2〜5秒, 全面フラッシュ, ゴースト無し。電池モードの毎回 / 給電モードの定期クリーンアップに使用
- **Partial refresh**: 高速・無点滅だがゴースト蓄積。給電モードの毎分時計更新に使用。N回ごとに full を1回挟んでゴーストをリセット
- deep sleep を跨ぐと partial は使えない（前フレーム前提のため）→ 電池モードは常に full

## 10. 実装マイルストーン（ハードウェア先行・段階的）

| # | 内容 | 完了条件 |
|---|---|---|
| M0 | PlatformIO 環境 + ボード書き込み確認 | Lチカ/シリアル出力が出る |
| M1 | GxEPD2 で "Hello" 全面描画 | パネルに文字が出る |
| M2 | WiFiプロビジョニング（キャプティブポータル）+ NVS保存 + 自動再接続 + NTP同期 + 日付時刻描画 | スマホ設定後に時刻が出る・電源再投入で自動再接続 |
| M3 | Open-Meteo 取得 → HERO（気温/概況/最高最低/3日予報）描画 | 実データの天気が出る |
| M4 | フォント組込み（Poppins/LINE Seed JP ビットマップ） + 案2 全レイアウト描画 | モック通りの全画面 |
| M5 | RSS ニュース + ローテーション（天気↔ニュース） | 右カラムが切り替わる |
| M6 | 電源モード自動切替 + deep sleep + partial時計更新 | 給電/電池どちらでも正しく動作 |

## 11. 未決 / 今後の検討

- ニュースRSSの具体的なフィード選定（NHK等）と取得頻度
- 「今日は何の日 / 名言 / 脳トレ漢字」のコンテンツ調達方法（ベイク vs オンライン）
- 地点設定（緯度経度）の持ち方 — WiFiManager のキャプティブポータルに**地点入力欄を追加**して一緒に設定するのが有力（要検討）
- 電池モードの起床間隔の最終調整（15分で良いか、夜間は伸ばすか）
